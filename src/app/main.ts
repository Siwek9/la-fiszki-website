import {createApp} from 'vue';
import App from './App.vue';
import 'overlayscrollbars/overlayscrollbars.css';

createApp(App).mount('#app');

// TODO app should prevent from reloading when there is no auto save on
